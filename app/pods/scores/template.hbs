<div class="container">
  <h2>Scores</h2>

  <div>
    {{#each model as |score|}}
      <div class="score">
        {{score.user.name}} - {{score.user.email}} - {{score.duration}}
      </div>
    {{/each}}
  </div>
</div>
